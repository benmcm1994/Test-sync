const state = {
  count: 0
};

const mutations = {
  increment(state) {
    state.count++;
  }
};

const getters = {

}

const actions = {
    
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
